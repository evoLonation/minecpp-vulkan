export module render.surface;

import <vulkan_config.h>;
import render.resource;
import render.tool;

import <glfw_config.h>;
import glfw;

import std;
import toy;

export namespace rd {

namespace extensions {
auto surface = std::vector<std::string>{
  "VK_KHR_surface",
#ifdef PLATFORM_WINDOWS
  "VK_KHR_win32_surface",
#elif PLATFORM_MACOS
  VK_EXT_METAL_SURFACE_EXTENSION_NAME,
#endif
  "VK_EXT_surface_maintenance1",
  "VK_KHR_get_surface_capabilities2",
};
} // namespace extensions

/**
 * @brief Create a Surface object, need created instance first
 *
 * @param window
 * @return rs::Surface
 */
auto createSurface(glfw::Window& window) -> rs::Surface {
  // these three lines is for debug
  // VkSurfaceKHR surface;
  // glfwCreateWindowSurface(rs::Instance::getInstance(), window, nullptr, &surface);
  // glfw::checkGlfwError();
  // implicit use instance
  return rs::Surface{ window.get() };
}

} // namespace rd